# Simulated Annealing hyperparameters
# Temperature schedule and neighbor generation settings.

[algorithm]
name = "simulated_annealing"
mutation_rate = 0.25
initial_temperature = 1.0
cooling_rate = 0.95
min_temperature = 0.01
max_generations = 200
