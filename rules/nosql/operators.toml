# NoSQL Operator Alternatives
# Grammar-aware payload mutations for MongoDB, Elastic, Redis, and Cassandra

# MongoDB logical / comparison operator variations
[[mongo_operator]]
pattern = "$ne"
description = "Not equal"

[[mongo_operator]]
pattern = "$nin"
description = "Not in array"

[[mongo_operator]]
pattern = "$eq"
description = "Equal"

[[mongo_operator]]
pattern = "$gt"
description = "Greater than"

[[mongo_operator]]
pattern = "$gte"
description = "Greater than or equal"

[[mongo_operator]]
pattern = "$regex"
description = "Regular expression match"

[[mongo_operator]]
pattern = "$exists"
description = "Field existence check"

[[mongo_operator]]
pattern = "$expr"
description = "Aggregation expression"

# Elastic query DSL variations
[[elastic_operator]]
pattern = "match"
description = "Standard match query"

[[elastic_operator]]
pattern = "match_phrase"
description = "Match phrase query"

[[elastic_operator]]
pattern = "query_string"
description = "Query string query"

[[elastic_operator]]
pattern = "term"
description = "Exact term query"

[[elastic_operator]]
pattern = "exists"
description = "Exists query"

# Redis command variations
[[redis_command]]
pattern = "EVAL"
description = "Lua script evaluation"

[[redis_command]]
pattern = "SCRIPT LOAD"
description = "Load Lua script"

[[redis_command]]
pattern = "CONFIG GET"
description = "Configuration retrieval"

[[redis_command]]
pattern = "INFO"
description = "Server info"
