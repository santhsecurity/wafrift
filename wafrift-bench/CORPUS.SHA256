# Pinned SHA-256 of the wafrift-bench corpus tree.
#
# Generated by:
#   find wafrift-bench/corpus -type f -name "*.toml" | sort | xargs sha256sum | sha256sum
#
# CI verifies this hash before publishing bench results JSON so a
# corpus drift cannot silently invalidate a published bypass-rate
# number. Update both this file AND any benchmark JSON in the same
# commit.
#
# Re-pinned 2026-06-09: commit d7eaf60 ("land evolved working tree")
# added the SSI payload class (corpus/ssi/apache_mod_include.toml) but
# did NOT re-pin in the same commit, so the gate went red. The SSI
# addition is intentional; this re-pins to the current legitimate tree
# (51 corpus TOMLs). NOTE for maintainers: bench results JSON produced
# before d7eaf60 reflect the pre-SSI corpus and should be re-validated
# against this tree when next republished.
#
# Prior re-pin 2026-05-19 (af5f5b2a…): commit b8d6e96 expanded the
# corpus with the SSRF/auth-bypass classes without re-pinning.
9929a7769be120b34cc38afe0fd2f30770fac03aebfd26d4a4002d20b3330973
