# wafrift-core

Facade crate that re-exports the focused WAF Rift crates through a single public API.

Use this crate when an integration wants the full evasion engine surface without depending on each subcrate directly.
