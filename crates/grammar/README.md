# wafrift-grammar

Grammar-aware payload mutation for SQL injection, XSS, command injection, SSRF, path traversal, LDAP, and template injection payloads.

Rules stay data-driven where practical so community-maintained TOML rule files can extend behavior without changing Rust code.
