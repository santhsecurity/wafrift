# wafrift-proxy

HTTP forward proxy that applies WAF Rift evasion strategies to outbound requests.

The proxy tracks per-host evasion state and can optionally support TLS interception primitives for local testing workflows.
