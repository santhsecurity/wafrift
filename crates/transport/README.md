# wafrift-transport

Transport adapters for applying WAF Rift evasion through HTTP client flows.

The crate integrates strategy execution with request sending and keeps optional integrations behind explicit Cargo features.
